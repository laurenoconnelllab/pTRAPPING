% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/limma_de.R
\name{limma_de}
\alias{limma_de}
\title{Differential expression analysis using limma}
\usage{
limma_de(
  expr_mat,
  group,
  contrast_mat,
  coefs = NULL,
  model_type = c("pairwise", "reference"),
  test_method = c("treat", "eBayes"),
  lfc_threshold = 1,
  fdr_threshold = 0.05,
  feature_col = "feature",
  ngenes_out = 20,
  kable_out = FALSE,
  kable_coef = 1
)
}
\arguments{
\item{expr_mat}{A numeric matrix with features (genes / proteins) as rows
and samples as columns. Row names must be feature identifiers and column
names must be sample IDs.}

\item{group}{A character or factor vector of group labels, one element per
column of \code{expr_mat}. The order must match the columns of \code{expr_mat}.}

\item{contrast_mat}{A contrast matrix produced by
\code{\link[limma:makeContrasts]{limma::makeContrasts()}}, \strong{or} a character vector of contrast strings
(e.g. \code{c("groupA-groupB", "groupB-groupC")}) which will be passed to
\code{\link[limma:makeContrasts]{limma::makeContrasts()}} internally. Contrast names must match the column
names of the design matrix (e.g. \code{"groupA"}, \code{"groupB"}, ...).}

\item{coefs}{An integer vector specifying which columns of \code{contrast_mat} to
extract results for. If \code{NULL} (default), all contrasts are used.}

\item{model_type}{One of \code{"pairwise"} (no intercept, \code{~ 0 + group},
recommended for multi-group designs with explicit contrasts) or
\code{"reference"} (first level as reference, \code{~ group}). See the
\emph{Choosing model_type} section. Default is \code{"pairwise"}.}

\item{test_method}{One of \code{"treat"} or \code{"eBayes"}. See the \emph{Choosing
test_method} section for guidance. Default is \code{"treat"}.}

\item{lfc_threshold}{Minimum absolute log2 fold change. Passed to
\code{treat(lfc = lfc_threshold)} when \code{test_method = "treat"}, and used as a
filter threshold for both methods. Default is \code{1}.}

\item{fdr_threshold}{Maximum adjusted p-value for a feature to be
classified as differentially expressed. Default is \code{0.05}.}

\item{feature_col}{Name of the column that will hold feature identifiers
(i.e. the row names of \code{expr_mat}) in the output tables. Default is
\code{"feature"}.}

\item{ngenes_out}{Number of top features to display when
\code{kable_out = TRUE}. Default is \code{20}.}

\item{kable_out}{Logical. If \code{TRUE}, returns an HTML \code{kableExtra} table for
a single contrast (indexed by \code{kable_coef}) instead of the full list.
Default is \code{FALSE}.}

\item{kable_coef}{Integer giving the position within \code{coefs} of the
contrast to display when \code{kable_out = TRUE}. Default is \code{1} (first
selected contrast).}
}
\value{
When \code{kable_out = FALSE} (default), a named list with:
\describe{
\item{\code{de_list}}{A named list of tibbles, one per selected contrast,
containing only DE features (filtered by \code{lfc_threshold} and
\code{fdr_threshold}). Each tibble includes \code{feature_col}, \code{logFC},
\code{adj.P.Val}, \code{contrast}, and \code{diffexpressed} (\code{"UP"} or \code{"DOWN"}).}
\item{\code{all_de}}{All per-contrast DE tibbles from \code{de_list} combined into
one tibble with \code{\link[dplyr:bind_rows]{dplyr::bind_rows()}}. A feature appears once per
contrast in which it is significant.}
\item{\code{shared}}{Tibble of features that are DE in \strong{opposite directions}
across different contrasts (min logFC < 0 and max logFC > 0). Useful
for flagging features whose direction of change is not consistent.}
\item{\code{unique}}{Tibble counting consistently DE features by direction
(\code{"UP"} or \code{"DOWN"}) — features absent from \code{shared}.}
\item{\code{fit}}{The fitted \code{MArrayLM} object returned by \code{treat()} or
\code{eBayes()}, for downstream use (e.g. calling \code{topTreat()} or
\code{topTable()} with additional contrast columns).}
}
When \code{kable_out = TRUE}, an HTML \code{kableExtra} table of the top
\code{ngenes_out} features for the selected contrast.
}
\description{
Runs a complete limma workflow on a feature-by-sample expression or
abundance matrix: builds a design matrix from a group vector, fits a linear
model, applies contrasts, and tests for differential expression using either
\code{\link[limma:ebayes]{limma::treat()}} or \code{\link[limma:ebayes]{limma::eBayes()}}. Returns a list containing
per-contrast DE tables, a combined table of all significant features, a
summary of features with inconsistent directions across contrasts, and
direction counts.
}
\section{Choosing \code{model_type}}{

\describe{
\item{\code{"pairwise"}}{Fits \code{model.matrix(~ 0 + group)}. No intercept; every
group gets its own coefficient. Use when you want explicit pairwise
contrasts (e.g. \code{groupA - groupB}). Requires a contrast matrix.}
\item{\code{"reference"}}{Fits \code{model.matrix(~ group)}. The first factor level
is the intercept / reference group; other coefficients represent
differences from it. Simpler when you have one natural reference
condition and only a few comparisons.}
}
}

\section{Choosing \code{test_method}}{

\describe{
\item{\code{"treat"}}{Calls \code{\link[limma:ebayes]{limma::treat()}}, which tests against a \strong{minimum
fold-change threshold} (\code{lfc_threshold}) rather than zero. Recommended
for proteomics or metabolomics data where you want to ensure biological
as well as statistical significance. Results are extracted with
\code{\link[limma:toptable]{limma::topTreat()}}.}
\item{\code{"eBayes"}}{Calls \code{\link[limma:ebayes]{limma::eBayes()}}, the standard empirical Bayes
moderation. Tests against a null of logFC = 0. Appropriate for RNA-seq
(after \code{voom}) or any analysis where no minimum fold-change is required.
Results are extracted with \code{\link[limma:toptable]{limma::topTable()}}.}
}
}

\examples{
\dontrun{
# --- pairwise design with treat() (proteomics / metabolomics) ---
group_vec <- rep(c("groupA", "groupB", "groupC"), each = 3)

# build the contrast matrix for selected pairwise comparisons
df_design <- data.frame(group = factor(group_vec))
design_mat <- model.matrix(~ 0 + group, data = df_design)
cont_mat <- limma::makeContrasts(
  groupA - groupB,
  groupA - groupC,
  levels = design_mat
)

res <- limma_de(
  expr_mat      = my_matrix,
  group         = group_vec,
  contrast_mat  = cont_mat,
  coefs         = 1:2,
  test_method   = "treat",
  lfc_threshold = 1,
  fdr_threshold = 0.05
)

res$de_list    # named list, one tibble per contrast
res$all_de     # all contrasts combined
res$shared  # features with inconsistent direction
res$unique  # count of consistently UP / DOWN features

# --- single comparison, reference group design with eBayes() ---
res2 <- limma_de(
  expr_mat     = my_matrix,
  group        = group_vec,
  contrast_mat = "groupgroupB",   # coefficient name in ~ group design
  model_type   = "reference",
  test_method  = "eBayes"
)
}

}
